#include "led_matrix.h"
#include "tetris.h"
#include <samd21.h>

void led_matrix_init(void) {
    /* TODO: configure SPI and LED matrix driver */
}

void led_matrix_draw(const uint8_t *board) {
    /* TODO: send board state via SPI to LED matrix */
}
